name: "Make Release"

on:
  push:
    branches:
      - "main"

defaults:
  run:
    working-directory: ./site_electron

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test"
