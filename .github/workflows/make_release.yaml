name: "Make Release"

on:
  push:
    paths:
      - "site_electron/release/*"

defaults:
  run:
    working-directory: ./site_electron

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test"
  
  